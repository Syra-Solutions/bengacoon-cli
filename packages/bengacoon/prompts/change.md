---
description: Run a change through routing, the smallest change verified the way the project's test mode sets, the reviews the gate names, and a commit carrying the evidence
argument-hint: "<what to fix, add, or change>"
---

Load the `change-router` skill and follow it for this request, start to finish.

The request:

$ARGUMENTS

Reach the end of the sequence rather than stopping at a working change. Announce
the route and the test mode first, run `prove-red` rather than reverting by hand
whenever the mode writes tests, and paste real output at every step — a summary
of a command is not its output.

If a step cannot be done, say which and why and stop there. Stopping with a
reason is a result; continuing quietly is not.
