# Sidebar status cards

- [x] render the status snapshot as sidebar cards
      done: at sidebar width, the status snapshot output contains titled sections with labeled values for git, usage/context, jobs, and quota instead of one raw text block
