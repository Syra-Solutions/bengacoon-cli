# Codex quota details

- [x] retain every Codex plan and quota field
      done: a usage payload with a plan, main and additional limits, two windows, reset times, and a reached flag produces a validated status snapshot with those fields

- [x] render every quota window as a usage gauge
      done: a sidebar snapshot with main and additional limits renders each window's used percentage as a ten-cell gauge, alongside its remaining percentage, reset time, and reached state
