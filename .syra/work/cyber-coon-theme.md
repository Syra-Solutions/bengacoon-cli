# Cyber Coon theme

- [x] expose the cyber-coon built-in theme
      done: getAvailableThemes() includes "cyber-coon", getDefaultTheme() returns "cyber-coon", and getThemeByName("cyber-coon") resolves its green accent token

- [ ] apply theme tokens to sidebar status cards
      done: when cyber-coon is active, sidebar card headings use the accent token and labels use the muted token
